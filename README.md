# claimlens

Skeleton project structure.

## Layout

- `src/agents/` — agent implementations
- `src/mcp/` — MCP server(s)
- `src/schemas/` — data schemas
- `evals/` — evaluation suites
- `data/fixtures/` — test/dev fixtures
- `.claude/` — Claude Code project config
- `docs/` — documentation
